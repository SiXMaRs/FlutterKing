import 'package:flutter/material.dart';

class ProfileLessPage extends StatelessWidget {
  const ProfileLessPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Stateless-Profile')),
      body: const Center(child: Text('Stateless-Profile')),
    );
  }
}
